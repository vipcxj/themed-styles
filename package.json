{
  "name": "themed-styles-librarys",
  "private": true,
  "scripts": {
    "bulid": "lerna run build",
    "lint": "lerna run lint",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2"
  }
}
